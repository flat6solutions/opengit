name: publish
run-name: "${{ format('release {0}', inputs.bump) }}"

on:
  workflow_dispatch:
    inputs:
      bump:
        description: "Bump major, minor, or patch"
        required: false
        type: choice
        options:
          - major
          - minor
          - patch
      version:
        description: "Override version (optional)"
        required: false
        type: string

concurrency: ${{ github.workflow }}-${{ github.ref }}-${{ inputs.version || inputs.bump }}

permissions:
  id-token: write
  contents: write
  packages: write

jobs:
  version:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    if: github.repository == 'flat6solutions/opengit'
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: oven-sh/setup-bun@v2

      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"

      - uses: ./.github/setup-git-committer
        id: committer
        with:
          opengit-app-id: ${{ secrets.OPENGIT_APP_ID }}
          opengit-app-secret: ${{ secrets.OPENGIT_APP_SECRET }}

      - name: Install Dependencies
        run: bun install

      - name: Version
        id: version
        run: ./script/version.ts
        env:
          GH_TOKEN: ${{ steps.committer.outputs.token }}
          OPENGIT_BUMP: ${{ inputs.bump }}
          OPENGIT_VERSION: ${{ inputs.version }}
          GH_REPO: ${{ github.repository }}

    outputs:
      version: ${{ steps.version.outputs.version }}
      release: ${{ steps.version.outputs.release }}
      tag: ${{ steps.version.outputs.tag }}
      repo: ${{ steps.version.outputs.repo }}

  build-cli:
    needs: version
    runs-on: blacksmith-4vcpu-ubuntu-2404
    if: github.repository == 'flat6solutions/opengit'
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: oven-sh/setup-bun@v2

      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"

      - name: Install Dependencies
        run: bun install

      - name: Setup git committer
        id: committer
        uses: ./.github/setup-git-committer
        with:
          opengit-app-id: ${{ secrets.OPENGIT_APP_ID }}
          opengit-app-secret: ${{ secrets.OPENGIT_APP_SECRET }}

      - name: Build
        id: build
        run: |
          ./packages/cli/scripts/build.ts --all
        env:
          OPENGIT_VERSION: ${{ needs.version.outputs.version }}
          OPENGIT_RELEASE: "true"
          GH_REPO: ${{ needs.version.outputs.repo }}
          GH_TOKEN: ${{ steps.committer.outputs.token }}

      - uses: actions/upload-artifact@v4
        with:
          name: opengit-cli
          path: packages/cli/dist

    outputs:
      version: ${{ needs.version.outputs.version }}

  publish:
    needs:
      - version
      - build-cli
    runs-on: blacksmith-4vcpu-ubuntu-2404
    if: github.repository == 'flat6solutions/opengit'
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: oven-sh/setup-bun@v2

      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"

      - name: Setup Git Identity
        run: |
          git config --global user.email "kustovsteven@gmail.com"
          git config --global user.name "opengit"

      - name: Install Dependencies
        run: bun install

      - uses: actions/download-artifact@v4
        with:
          name: opengit-cli
          path: packages/cli/dist

      - name: Publish
        run: ./script/publish.ts
        env:
          OPENGIT_VERSION: ${{ needs.version.outputs.version }}
          OPENGIT_RELEASE: "true"
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ needs.version.outputs.repo }}
